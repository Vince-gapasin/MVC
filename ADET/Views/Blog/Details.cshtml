@model YourProjectName.Models.Post

@{
    ViewData["Title"] = Model.Title;
}

<div class="card mt-4">
    <div class="card-body">
        <h2 class="card-title">@Model.Title</h2>
        <h6 class="card-subtitle mb-2 text-muted">By: @Model.Author</h6>
        <hr />
        <p class="card-text">@Model.Content</p>
        
        <a asp-controller="Blog" asp-action="Index" class="btn btn-secondary mt-3">Back to List</a>
    </div>
</div>